export class AuthController {
    constructor() {}

    /**
     *
     * @param {*} req
     * @param {*} res
     */
    async login(req, res) {
        const result = req.body;
    }

    /**
     *
     * @param {*} req
     * @param {*} res
     */
    async register(req, res) {}
}
